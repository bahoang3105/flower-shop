import { Row } from 'antd';
import React from 'react';

function AppCarousel(props: any) {
  const { list, numberItemPerView, slidesPerView } = props || {};
  return (
    <div>
      <Row></Row>
    </div>
  );
}

export default AppCarousel;
